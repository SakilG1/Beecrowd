#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t,n,i;
    long long int x;
    cin >> t;
    while(t--)
    {
        x=0;
        cin >> n;
        for(i=0; i<n; i++)
        {
            x=x+pow(2, i);
        }
        cout << x << endl;
    }

    return 0;
}
