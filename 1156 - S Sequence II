#include<bits/stdc++.h>
using namespace std;
int main()
{
    double s=0.0, i, j;
    for(i=1, j=0; i<=39; i+=2)
    {
        s += i/pow(2, j);
        j++;
    }

    cout << fixed << setprecision(2) << s << endl;
    return 0;
}
