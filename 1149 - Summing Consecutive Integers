#include<bits/stdc++.h>
using namespace std;
int main()
{
    int a, n, sum=0;
    cin >> a >> n;
    while(n<=0)
    {
        cin >> n;
    }
    while(n--)
    {
        sum += a;
        a++;
    }
    cout << sum << endl;

    return 0;
}
