#include<bits/stdc++.h>
using namespace std;
int main()
{
    int a,b,c,x;
    while(cin >> a >> b >> c)
    {
        if(a==0 && b==0 && c==0)
            break;

        x=cbrt(a*b*c);

        cout << x << endl;
    }

    return 0;
}
