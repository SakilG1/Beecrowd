#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t, n;
    while(cin >> t >> n && t!=0)
    {
        int x,c=0;
        string s;
        while(t--)
        {
            cin >> s >> x;
            c += x;
        }
        cout << (n*3)-c << endl;

    }

    return 0;
}
