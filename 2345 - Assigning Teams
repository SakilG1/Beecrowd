#include<bits/stdc++.h>
using namespace std;
int main()
{
    int arr[4],x,y;
    cin >> arr[0] >> arr[1] >> arr[2] >> arr[3];
    sort(arr,arr+4);
    x=arr[0]+arr[3];
    y=arr[1]+arr[2];

    cout << (abs(x-y)) << "\n";

    return 0;

}
