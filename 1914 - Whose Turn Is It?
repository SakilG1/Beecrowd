#include <bits/stdc++.h>
using namespace std;

#define lli long long int
#define no cout << "NO" << endl
#define yes cout << "YES" << endl
#define inkj i, j, n, m, k, x, y, z
#define fn for (i = 0; i < n; i++)
#define arr(n)  \
    lli arr[n]; \
    fn { cin >> arr[i]; }
lli t = 1;
void crack()
{
    lli inkj;
    string s1, s2, s3, s4;
    cin >> s1 >> s2 >> s3 >> s4;
    cin >> n >> m;
    n = n+m;
    if(n%2==0)
    {
        if(s2[0]=='P')
        {
            cout << s1 << endl;
        }
        else{
            cout << s3 << endl;
        }
    }
    else{
        if(s2[0]=='I')
        {
            cout << s1 << endl;
        }
        else{
            cout << s3 << endl;
        }
    }
}

int main()
{
    lli test_case = 1;
    cin >> test_case;
    while (test_case--)
    {
        crack();
    }
    return 0;
}
