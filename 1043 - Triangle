#include<bits/stdc++.h>
using namespace std;
int main()
{
    float A,B,C;
    cin >> A >> B >> C;
    if(A<B+C && B<A+C && C<A+B)
    {
        printf("Perimetro = %.1f\n",A+B+C);
    }
    else
    {
        printf("Area = %.1f\n",((A+B)/2)*C);
    }

    return 0;
}
