#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        double a, b, c, x;
        cin >> a >> b >> c;
        x = ((a*2)+(b*3)+(c*5))/10;
        cout << fixed << setprecision(1) << x << endl;
    }

    return 0;
}
