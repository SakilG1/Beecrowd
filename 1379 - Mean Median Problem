#include<bits/stdc++.h>
using namespace std;
int main()
{
    int a,b,x;
    while(cin >> a >> b)
    {
        if(a==0 && b==0)
            break;
        x=min(a,b)-abs(a-b);
        cout << x << endl;
    }

    return 0;
}
