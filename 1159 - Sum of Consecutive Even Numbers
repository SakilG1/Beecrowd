#include<bits/stdc++.h>
using namespace std;
int main()
{
    int x;
    while(cin >> x && x!=0)
    {
        if(x%2!=0)
            x++;
        int n=5, s=0;
        while(n--)
        {
            s += x;
            x += 2;
        }
        cout << s << endl;
    }

    return 0;
}
