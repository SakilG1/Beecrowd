#include<bits/stdc++.h>
using namespace std;
int main()
{
    string s;
    while( cin >> s && s[0]!='0')
    {
        unsigned long long int x, r=1;
        x = s.length();
        for(int i=1; i<=x; i++)
        {
            r = r*i;
        }
        cout << r << endl;
    }
    return 0;
}

